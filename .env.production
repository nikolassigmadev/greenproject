# OpenAI API key is now configured server-side via Netlify environment variables.
# Set OPENAI_API_KEY in your Netlify site settings (Site > Configuration > Environment variables).
# Do NOT put API keys in this file — they get embedded in the browser bundle.
